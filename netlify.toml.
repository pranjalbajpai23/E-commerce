[build]
  command = "npm run build"  # Change this to your build script
  publish = "client/dist"   # Change this to your build output directory

[[redirects]]  # Redirects for API routes
  from = "/items/*"
  to = "http://localhost:8080/items/"
  status = 200

